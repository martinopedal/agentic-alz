# ADR <NNNN>: <short title>

- **Status:** Proposed | Accepted | Superseded by ADR-XXXX
- **Date:** YYYY-MM-DD
- **Authors:** <names>
- **Generated by:** Design stage `prompts/design.v1.md` (or "human")
- **Inputs commit:** `<sha of inputs.yaml>`
- **Design commit:** `<sha of design.json>`

## Context

What is the situation? What constraints (regulatory, organisational,
technical) apply? Cite Microsoft Learn URLs returned by `microsoft-learn-mcp`
where relevant — and note that those citations are advisory, not
authoritative.

## Decision

What did we decide and why? Be specific about the option chosen *and* the
options rejected.

## Constraints honoured

- AVM modules: only sources/versions in `templates/<topology>/versions.lock`.
- ALZ conformance: passes `policies/alz_conformance.rego`.
- Naming: matches the convention in `inputs.yaml > naming`.

## Consequences

- Operational impact (Day-2 runbook deltas).
- Cost impact (Infracost monthly delta from baseline).
- Security impact (new exposure surface, new identities, new policy effects).
- Reversibility (how hard is this to undo?).

## Validation evidence

- Plan summary: `<link to plan artifact>`.
- Risk report: `<link to risk.json>`.
- Conformance result: `<link to conftest output>`.

## Follow-ups

- [ ] List concrete tickets to file.
